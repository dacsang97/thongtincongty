const distPath = `${__dirname}/../dist`;

const config = {
  BASE_URL: 'http://www.thongtincongty.com',
  DIST_PATH: `${distPath}`,
  IMG_DIST_PATH: `${distPath}/imgs`,
  MAX_PAGE: 17536,
  MAX_CONNECT: 10,
  DB_PATH: `${distPath}/db.json`,
};

export default config;
